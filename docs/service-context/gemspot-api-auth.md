# gemspot-api auth — moved

This file is a redirect. The `/auth` HTTP surface is now documented authoritatively in
[HTTP.md](HTTP.md); transport presence is in [API.md](API.md); persistence (users,
refresh_tokens, email_change_tokens) in [DATA.md](DATA.md); the mail dependency in
[DEPENDENCIES.md](DEPENDENCIES.md). Kept only as a redirect to avoid drift.
