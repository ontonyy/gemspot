# Implementation Plans

Generated by the improve skill on 2026-06-28 (advisor pass against commit `fdbc18f`).

Implementation plans for the GemSpot monorepo (`web/` React frontend + `api/` Spring Boot
service). Plan 001 was docs-only; everything since changes source. Each executor: read the
plan fully before starting, honor its STOP conditions, run every Verify command, and update
the table below.

Executor rules:
- Read the whole plan before touching anything.
- Run the **Drift check** first. STOP on mismatch — plans were written against an older
  commit and the cited code may have moved.
- Never reproduce secret values — reference `file:line` + the key name only.
- `docs/service-context/*` is generated: only edit inside the
  `<!-- service-context:auto:start -->` … `<!-- service-context:auto:end -->` markers, and
  preserve human prose around them.
- Both deploys are gated on lint + tests (plan 004). Green suites before you open a PR, and
  rebase on master first — a branch behind master can fail the gate on the merged tree.
- Move the plan into `done/` and update the table when it lands.

## Layout

Open plans sit at the top level of `plans/`. Completed ones move to
[`plans/done/`](done/) — kept, not deleted, because several carry diagnosis and rationale
that the commit messages don't (notably 011's root-cause analysis and 004's justification
for each lint suppression).

## Open

| Plan | Title | Priority | Effort | Risk | Depends on | Status |
|------|-------|----------|--------|------|------------|--------|
| [002](002-fix-n-plus-1-queries.md) | Eliminate N+1 query patterns in list endpoints | P2 | M | MED | — | TODO |
| [003](003-validate-geo-coordinates.md) | Validate latitude/longitude on submission input | P2 | S | LOW | — | TODO |
| [005](005-web-auth-refresh-tests.md) | Test the web 401→refresh→retry auth seam | P3 | M | LOW | — | TODO |

Suggested order: **003** (smallest, independent) → **002** (needs its own regression tests)
→ **005**. Drift checks for all three passed at `240bfc0`.

## Done — [`plans/done/`](done/)

001–005 came from the advisor audit; 006+ from design review and user bug reports.

| Plan | Title | Landed |
|------|-------|--------|
| [001](done/001-service-context-docs.md) | Canonical service-context doc set | `aad27f1`, 2026-06-29 |
| [004](done/004-ci-lint-test-gates.md) | Lint + test gates in CI before deploy | `45e7905`, PR #25 |
| [006](done/006-spot-detail-duplicate-category-label.md) | Spot detail: duplicate category label on photo-less spots | `9dd8912` |
| [007](done/007-spot-detail-action-links-layout.md) | Spot detail: action links as separate rows | `58d42a4` |
| [008](done/008-spot-detail-share-copy-link.md) | Spot detail: share menu with explicit copy link | `9a78d77` |
| [009](done/009-field-notes-clarity.md) | Field notes: human-readable labels | `cb615e3` |
| [010](done/010-static-info-pages.md) | Static info pages + footer link fixes | `77ecd7e` |
| [011](done/011-add-spot-submission-pipeline.md) | "Add a spot": submit failures + admin queue 500 | `7c2f235`, PR #26 |
| [012](done/012-add-spot-location-picker-ux.md) | Add-spot location picker UX | `7342cc0` |

## Dependency notes

- 002, 003 and 005 are mutually independent — any order, parallel branches fine.
- 005's soft dependency on 004 is satisfied: the gate exists, so new Vitest tests block
  deploys as soon as they land.

## Code findings (full audit, 2026-07-03)

Second pass fanned out correctness/security/perf/tests across `api/` and `web/`. Vetted
findings below (each read + confirmed in source). Plans written for the high-leverage ones.

| ID | Severity | Type | file:line | Finding | Plan |
|----|----------|------|-----------|---------|------|
| C1 | MED | perf | `api/.../service/PlacesService.java:31-40` (+ `Place.categories` LAZY) | `GET /places` lazily loads categories per place (N+1) on the hottest endpoint | 002 |
| C2 | LOW-MED | perf | `api/.../service/AdminService.java:250-264` | `listUsers()` runs one profile query per user | 002 |
| C3 | LOW-MED | perf | `api/.../service/SubmissionsService.java:60-71` | `listMine()` runs one photo query per submission | 002 |
| C4 | MED | bug (data integrity) | `api/.../dto/SubmissionInputDto.java:15-16` | `lat`/`lng` accept any double — no range bounds; invalid coords persist | 003 |
| C5 | MED | dx | `.github/workflows/deploy-web.yml:26-30`, `deploy-api.yml:38-41` | Deploys run no lint/test gate; regressions ship | 004 |
| C6 | HIGH (coverage) | tests | `web/src/shared/api/httpPlacesApi.ts:21-35`, `authStore.ts` | 401→refresh→retry + refresh dedup: zero web tests (only 2 util tests exist) | 005 |

## Findings considered and rejected

- **JWT tokens in `localStorage`** (`web/src/shared/store/authStore.ts`): deliberate SPA choice
  (15m access TTL + server-side refresh-reuse detection). Known trade-off, documented in
  `OBSERVABILITY.md`. Not a bug. See Direction options.
- **`/auth/email/verify` public**: by design — the UUID token *is* the authority. Not a finding.
- **OAuth tokens verified via outbound RestClient to Google/Facebook** (`AuthService.java`):
  standard provider verification, not SSRF. Not a finding.
- **`rejectSubmission()` lacks `@Transactional`** (`AdminService.java:173`) while
  `approveSubmission()` has it: **rejected** — reject is a single `save()` (already transactional
  at the repository boundary); the asymmetry is necessary (approve is multi-write), not a defect.
- **Mutable non-volatile `RestClient` field** (`AuthService.java:50`): the setter is a
  test-only seam and `RestClient` is thread-safe; no realistic production race. Rejected.
- **`dangerouslySetInnerHTML` for map attribution** (`web/.../LocationPicker.tsx:64`): strings
  are static vendor HTML, no user input. Anti-pattern, not a live XSS. Direction, not a plan.
- **Caret (`^`) dependency ranges** (`web/package.json`): standard; acceptable once CI tests
  exist (004). Not a finding.
- **`console.warn(err)` in SpotMap** (`SpotMap.tsx:185,197`): no token in the error object.
  Log-hygiene nit, not a leak. Rejected.
- **SpotMap cluster `active` stale-closure** (`SpotMap.tsx:393-417`): `renderCluster` captures
  `sel`/`focus` at call time; a selection change during async `getClusterLeaves()` can briefly
  show a stale cluster highlight, but self-heals on the next `updateMarkers()` (map move/data).
  Minor cosmetic, self-correcting — not worth a plan.
- **Separate `AGENTS.md`/`CLAUDE.md`**: a minimal `AGENTS.md` was created during 001 apply
  (repo had neither). Resolved, not pending.

## Direction options (maintainer's call — not bugs)

- **Token storage hardening**: move refresh token to an HttpOnly + SameSite cookie and
  keep only the short-lived access token in memory. Removes the localStorage XSS surface
  entirely. Trade-off: backend must set/read cookies and CORS `credentials` is already on,
  but it changes the auth seam in `web/src/shared/api` and `SecurityConfig`. Non-trivial;
  weigh against current short-TTL mitigation.
- ~~**Deploy-target source of truth**~~ — **RESOLVED 2026-08-06.** Cloud Run is authoritative;
  `api/README.md` now names service `gemspot-api` (`europe-north1`, project `gemspot-498821`)
  and the stale `onrender.com` reference is gone (landed in `7c2f235`). Note
  `docs/service-context/RUNBOOK.md` still carries the old "URL ambiguous" TODO inside its
  auto-generated block — clear it on the next service-context regen.
- **ADR for monorepo doc layout**: a single repo-root `docs/service-context/` covering two
  deployables (web + api) vs. per-package dirs. Plan 001 takes the root-level approach with
  a `WEB.md` for the frontend; if you prefer per-package, capture it as ADR 0003 first.
- **Split the Supabase S3 access key from the secret key** (security): `deploy-api.yml:44-45`
  notes the access key currently *equals* the secret key and is stored as a GH secret. Rotate
  to a distinct access/secret pair. Ops/config change (no source), not a code plan — no secret
  values are reproduced here; do it in Secret Manager + GH settings.
- **Admin route guard renders before auth hydration** (`web/.../pages/admin/AdminLayout.tsx:19-20`):
  on a cold load an admin can be bounced to `/auth` before the store hydrates, then must
  re-navigate. UX flicker, not an access bypass (no unauthorized data shown). Gate on a
  "hydrated" flag if it annoys users. Maintainer's call.

## What was NOT audited

- Deep security pen-test (authz fuzzing, token forgery, CORS abuse) — only static review done.
- Actual load/perf profiling with real query plans (N+1s found by code inspection, not measured).
- API test *quality* (assertion depth) — only presence/coverage gaps mapped.
- Map widget lifecycle beyond the cluster stale-closure (full memory-leak profiling not run).
- The archived legacy NestJS backend (`../legacy-nest/`).
- `design/`, `design_handoff_field_guide/`, `docs/mvp`, `docs/v2` content quality.
- Secret material in `keys/` and `.env` (intentionally untouched).
