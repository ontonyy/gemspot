node_modules/
dist/
.env
*.log

# uploaded photos (local-disk storage backend) — anchored to repo root so it
# does NOT match the src/api/uploads/ module.
/uploads/

# Gradle / Spring Boot
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar
