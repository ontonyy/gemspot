<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!--
      D4 — refresh-token reuse detection. Net-new table (not in Prisma schema).
      jti = PK (rotated per refresh); family_id groups a token lineage; replay of
      a used jti within a family kills the family (Block 5). snake_case columns.
    -->
    <changeSet id="0002-refresh-tokens" author="gemspot">
        <sql splitStatements="true" endDelimiter=";"><![CDATA[
            CREATE TABLE "refresh_tokens" (
                "jti" TEXT NOT NULL,
                "user_id" TEXT NOT NULL,
                "family_id" TEXT NOT NULL,
                "used" BOOLEAN NOT NULL DEFAULT false,
                "expires_at" TIMESTAMP(3) NOT NULL,
                "created_at" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
                CONSTRAINT "refresh_tokens_pkey" PRIMARY KEY ("jti")
            );

            CREATE INDEX "refresh_tokens_family_id_idx" ON "refresh_tokens"("family_id");
            CREATE INDEX "refresh_tokens_user_id_idx" ON "refresh_tokens"("user_id");

            -- user_id -> users.id (CASCADE: tokens die with the user)
            ALTER TABLE "refresh_tokens" ADD CONSTRAINT "refresh_tokens_user_id_fkey"
                FOREIGN KEY ("user_id") REFERENCES "users"("id") ON DELETE CASCADE ON UPDATE CASCADE;
        ]]></sql>
        <rollback>
            <sql>DROP TABLE "refresh_tokens";</sql>
        </rollback>
    </changeSet>

</databaseChangeLog>
