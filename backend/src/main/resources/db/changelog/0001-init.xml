<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!--
      Faithful port of backend/prisma/migrations/0001_init + 0002_user_oauth.
      Raw SQL preserves native PG enum types, text[] arrays, jsonb, and the exact
      camelCase column names Prisma emits (only table names are snake_case @@map).
      ddl-auto=validate: Liquibase owns DDL; Hibernate only checks the mapping.
    -->

    <!-- Enum types (PG CREATE TYPE; case-sensitive quoted names) -->
    <changeSet id="0001-enums" author="gemspot">
        <sql splitStatements="true" endDelimiter=";">
            CREATE TYPE "UserRole" AS ENUM ('CLIENT', 'ADMIN');
            CREATE TYPE "PlaceStatus" AS ENUM ('ACTIVE', 'INACTIVE', 'DRAFT');
            CREATE TYPE "SubmissionStatus" AS ENUM ('PENDING', 'APPROVED', 'REJECTED');
            CREATE TYPE "ReportStatus" AS ENUM ('OPEN', 'RESOLVED', 'DISMISSED');
            CREATE TYPE "ReportReason" AS ENUM ('CLOSED', 'WRONG_LOCATION', 'NOT_FREE', 'OTHER');
        </sql>
        <rollback>
            <sql splitStatements="true" endDelimiter=";">
                DROP TYPE "ReportReason";
                DROP TYPE "ReportStatus";
                DROP TYPE "SubmissionStatus";
                DROP TYPE "PlaceStatus";
                DROP TYPE "UserRole";
            </sql>
        </rollback>
    </changeSet>

    <!-- Tables (camelCase columns preserved; passwordHash nullable + provider
         columns reflect 0002_user_oauth collapsed in). -->
    <changeSet id="0001-tables" author="gemspot">
        <sql splitStatements="true" endDelimiter=";"><![CDATA[
            CREATE TABLE "users" (
                "id" TEXT NOT NULL,
                "email" TEXT NOT NULL,
                "passwordHash" TEXT,
                "provider" TEXT,
                "providerId" TEXT,
                "role" "UserRole" NOT NULL DEFAULT 'CLIENT',
                "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
                "updatedAt" TIMESTAMP(3) NOT NULL,
                CONSTRAINT "users_pkey" PRIMARY KEY ("id")
            );

            CREATE TABLE "profiles" (
                "id" TEXT NOT NULL,
                "userId" TEXT NOT NULL,
                "name" TEXT,
                "avatarUrl" TEXT,
                "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
                "updatedAt" TIMESTAMP(3) NOT NULL,
                CONSTRAINT "profiles_pkey" PRIMARY KEY ("id")
            );

            CREATE TABLE "categories" (
                "id" TEXT NOT NULL,
                "slug" TEXT NOT NULL,
                "label" TEXT NOT NULL,
                "short" TEXT NOT NULL,
                "cssvar" TEXT NOT NULL,
                "sort" INTEGER NOT NULL DEFAULT 0,
                CONSTRAINT "categories_pkey" PRIMARY KEY ("id")
            );

            CREATE TABLE "places" (
                "id" TEXT NOT NULL,
                "slug" TEXT NOT NULL,
                "name" TEXT NOT NULL,
                "neighborhood" TEXT NOT NULL,
                "lat" DOUBLE PRECISION NOT NULL,
                "lng" DOUBLE PRECISION NOT NULL,
                "status" "PlaceStatus" NOT NULL DEFAULT 'ACTIVE',
                "savesCount" INTEGER NOT NULL DEFAULT 0,
                "viewsCount" INTEGER NOT NULL DEFAULT 0,
                "sharesCount" INTEGER NOT NULL DEFAULT 0,
                "isFree" BOOLEAN NOT NULL DEFAULT true,
                "thumbUrl" TEXT,
                "tags" TEXT[],
                "note" TEXT NOT NULL,
                "contributorName" TEXT NOT NULL,
                "verifiedLabel" TEXT,
                "accessNote" TEXT NOT NULL,
                "litNote" TEXT NOT NULL,
                "bestNote" TEXT NOT NULL,
                "sort" INTEGER NOT NULL DEFAULT 0,
                "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
                "updatedAt" TIMESTAMP(3) NOT NULL,
                CONSTRAINT "places_pkey" PRIMARY KEY ("id")
            );

            CREATE TABLE "place_categories" (
                "id" TEXT NOT NULL,
                "placeId" TEXT NOT NULL,
                "categoryId" TEXT NOT NULL,
                "primary" BOOLEAN NOT NULL DEFAULT false,
                CONSTRAINT "place_categories_pkey" PRIMARY KEY ("id")
            );

            CREATE TABLE "place_photos" (
                "id" TEXT NOT NULL,
                "placeId" TEXT NOT NULL,
                "url" TEXT NOT NULL,
                "sort" INTEGER NOT NULL DEFAULT 0,
                CONSTRAINT "place_photos_pkey" PRIMARY KEY ("id")
            );

            CREATE TABLE "saved_places" (
                "id" TEXT NOT NULL,
                "userId" TEXT NOT NULL,
                "placeId" TEXT NOT NULL,
                "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
                CONSTRAINT "saved_places_pkey" PRIMARY KEY ("id")
            );

            CREATE TABLE "submissions" (
                "id" TEXT NOT NULL,
                "userId" TEXT,
                "name" TEXT NOT NULL,
                "categoryId" TEXT NOT NULL,
                "lat" DOUBLE PRECISION NOT NULL,
                "lng" DOUBLE PRECISION NOT NULL,
                "note" TEXT NOT NULL,
                "photoCount" INTEGER NOT NULL DEFAULT 0,
                "status" "SubmissionStatus" NOT NULL DEFAULT 'PENDING',
                "submittedAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
                CONSTRAINT "submissions_pkey" PRIMARY KEY ("id")
            );

            CREATE TABLE "submission_photos" (
                "id" TEXT NOT NULL,
                "submissionId" TEXT NOT NULL,
                "url" TEXT NOT NULL,
                "sort" INTEGER NOT NULL DEFAULT 0,
                CONSTRAINT "submission_photos_pkey" PRIMARY KEY ("id")
            );

            CREATE TABLE "reports" (
                "id" TEXT NOT NULL,
                "userId" TEXT,
                "placeId" TEXT,
                "placeSlug" TEXT NOT NULL,
                "placeName" TEXT NOT NULL,
                "reason" "ReportReason" NOT NULL,
                "note" TEXT,
                "status" "ReportStatus" NOT NULL DEFAULT 'OPEN',
                "reportedAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
                CONSTRAINT "reports_pkey" PRIMARY KEY ("id")
            );

            CREATE TABLE "events" (
                "id" TEXT NOT NULL,
                "name" TEXT NOT NULL,
                "props" JSONB,
                "placeId" TEXT,
                "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
                CONSTRAINT "events_pkey" PRIMARY KEY ("id")
            );
        ]]></sql>
        <rollback>
            <sql splitStatements="true" endDelimiter=";">
                DROP TABLE "events";
                DROP TABLE "reports";
                DROP TABLE "submission_photos";
                DROP TABLE "submissions";
                DROP TABLE "saved_places";
                DROP TABLE "place_photos";
                DROP TABLE "place_categories";
                DROP TABLE "places";
                DROP TABLE "categories";
                DROP TABLE "profiles";
                DROP TABLE "users";
            </sql>
        </rollback>
    </changeSet>

    <!-- Unique indexes + the events name index -->
    <changeSet id="0001-indexes" author="gemspot">
        <sql splitStatements="true" endDelimiter=";">
            CREATE UNIQUE INDEX "users_email_key" ON "users"("email");
            CREATE UNIQUE INDEX "users_provider_providerId_key" ON "users"("provider", "providerId");
            CREATE UNIQUE INDEX "profiles_userId_key" ON "profiles"("userId");
            CREATE UNIQUE INDEX "categories_slug_key" ON "categories"("slug");
            CREATE UNIQUE INDEX "places_slug_key" ON "places"("slug");
            CREATE UNIQUE INDEX "place_categories_placeId_categoryId_key" ON "place_categories"("placeId", "categoryId");
            CREATE UNIQUE INDEX "saved_places_userId_placeId_key" ON "saved_places"("userId", "placeId");
            CREATE INDEX "events_name_idx" ON "events"("name");
        </sql>
    </changeSet>

    <!-- Foreign keys with documented onDelete (CASCADE = owned children;
         SET NULL = optional attribution survives parent delete). -->
    <changeSet id="0001-foreign-keys" author="gemspot">
        <sql splitStatements="true" endDelimiter=";">
            -- profiles.userId -> users.id  (CASCADE: profile is owned by user)
            ALTER TABLE "profiles" ADD CONSTRAINT "profiles_userId_fkey"
                FOREIGN KEY ("userId") REFERENCES "users"("id") ON DELETE CASCADE ON UPDATE CASCADE;
            -- place_categories.placeId -> places.id  (CASCADE)
            ALTER TABLE "place_categories" ADD CONSTRAINT "place_categories_placeId_fkey"
                FOREIGN KEY ("placeId") REFERENCES "places"("id") ON DELETE CASCADE ON UPDATE CASCADE;
            -- place_categories.categoryId -> categories.id  (CASCADE)
            ALTER TABLE "place_categories" ADD CONSTRAINT "place_categories_categoryId_fkey"
                FOREIGN KEY ("categoryId") REFERENCES "categories"("id") ON DELETE CASCADE ON UPDATE CASCADE;
            -- place_photos.placeId -> places.id  (CASCADE)
            ALTER TABLE "place_photos" ADD CONSTRAINT "place_photos_placeId_fkey"
                FOREIGN KEY ("placeId") REFERENCES "places"("id") ON DELETE CASCADE ON UPDATE CASCADE;
            -- saved_places.userId -> users.id  (CASCADE)
            ALTER TABLE "saved_places" ADD CONSTRAINT "saved_places_userId_fkey"
                FOREIGN KEY ("userId") REFERENCES "users"("id") ON DELETE CASCADE ON UPDATE CASCADE;
            -- saved_places.placeId -> places.id  (CASCADE)
            ALTER TABLE "saved_places" ADD CONSTRAINT "saved_places_placeId_fkey"
                FOREIGN KEY ("placeId") REFERENCES "places"("id") ON DELETE CASCADE ON UPDATE CASCADE;
            -- submissions.userId -> users.id  (SET NULL: keep submission if user deleted)
            ALTER TABLE "submissions" ADD CONSTRAINT "submissions_userId_fkey"
                FOREIGN KEY ("userId") REFERENCES "users"("id") ON DELETE SET NULL ON UPDATE CASCADE;
            -- submission_photos.submissionId -> submissions.id  (CASCADE)
            ALTER TABLE "submission_photos" ADD CONSTRAINT "submission_photos_submissionId_fkey"
                FOREIGN KEY ("submissionId") REFERENCES "submissions"("id") ON DELETE CASCADE ON UPDATE CASCADE;
            -- reports.userId -> users.id  (SET NULL)
            ALTER TABLE "reports" ADD CONSTRAINT "reports_userId_fkey"
                FOREIGN KEY ("userId") REFERENCES "users"("id") ON DELETE SET NULL ON UPDATE CASCADE;
            -- reports.placeId -> places.id  (SET NULL: keep report if place deleted; slug/name denormalized)
            ALTER TABLE "reports" ADD CONSTRAINT "reports_placeId_fkey"
                FOREIGN KEY ("placeId") REFERENCES "places"("id") ON DELETE SET NULL ON UPDATE CASCADE;
        </sql>
    </changeSet>

</databaseChangeLog>
